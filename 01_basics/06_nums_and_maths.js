// const score = 400
// // When we write this in google console then it automatically assumes that score is number.

// // We also have methods where we can specify that what we are declaring is actually a number.

// const balance = new Number(100)
// console.log(balance);

// we can derive its prototypes from console.

const otherNumber = 123.8933

console.log(otherNumber.toPrecision(4));

